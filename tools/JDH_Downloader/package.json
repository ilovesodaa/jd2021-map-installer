{
  "name": "jdh-downloader",
  "version": "1.0.0",
  "description": "Automates Discord bot queries to extract Just Dance asset HTML",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fetch": "node fetch.mjs",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}